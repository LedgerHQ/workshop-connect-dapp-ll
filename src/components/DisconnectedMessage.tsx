import { Flex, Text } from "@ledgerhq/react-ui";

const DisconnectedMEssage = () => (
  <Flex>
    <Text variant="body">⚠️ You must be logged in to run the DApp</Text>
  </Flex>
);

export default DisconnectedMEssage;
